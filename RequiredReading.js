function required(numPages, pagesRead, days) {
    let totalTime = numPages / pagesRead;
    console.log(totalTime / days);
}
required(212,
    20,
    2
);
required(432,
    15,
    4
);
